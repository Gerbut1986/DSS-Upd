

<h2 style="text-align:center;color:darkorchid">@ViewBag.ObjName (Records Found - <span style="color:gold">@ViewBag.Count</span>) for <span style="color:crimson">@ViewBag.LocInfo</span></h2>


@if (ViewBag.Check1 == true && ViewBag.EmptLocation == true)
{
    <h2 style="text-align:center;color:tomato;font-family:'Palatino Linotype';font-weight:bold">
        Summary For All Locations:
    </h2>
    <table class="table table-bordered" style="">
        <thead>
            <tr style="font-size:medium">
                <th scope="col">
                    <label>Code</label>
                </th>
                <th scope="col">
                    <label>Exercise</label>
                </th>
                <th scope="col">
                    <label>Method</label>
                </th>
                <th scope="col">
                    <label>Date</label>
                </th>
            </tr>
        </thead>
        <tbody>
            @{
                List<DSS.BLL.PartialModels.EmrgencySummaryAll> arr = ViewBag.TotalSummary;
                foreach (var ci in arr)
                {
                    <tr style="font-size:small;color:black">
                        <td scope="row">@ci.Code</td>
                        <td scope="row">@ci.Exercise</td>
                        <td scope="row">@ci.Method</td>
                        <td scope="row">@ci.Date</td>
                    </tr>
                }
            }
        </tbody>
    </table>

    foreach (var l in ViewBag.Locations)
    {
        <h2 style="text-align:center;color:darkorange;font-family:'Times New Roman'">@l</h2>
        <table class="table table-bordered" style="">
            <thead>
                <tr style="font-size:small">
                    <th scope="col">
                        <label>Code</label>
                    </th>
                    <th scope="col">
                        <label>Exercise</label>
                    </th>
                    <th scope="col">
                        <label>Method</label>
                    </th>
                    <th scope="col">
                        <label>Date</label>
                    </th>
                </tr>
            </thead>
            <tbody>
                @{
                    List<DSS.BLL.PartialModels.EmrgencySummary> list = ViewBag.GN_Found;
                    for (int ci = 0; ci < list.Count; ci++)
                    {
                        if (list[ci].LocationName == l)
                        {
                            <tr style="font-size:small;color:black">
                                <td scope="row">@Html.DisplayFor(m => list[ci].Code)</td>
                                <td scope="row">@Html.DisplayFor(m => list[ci].Exercise)</td>
                                <td scope="row">@Html.DisplayFor(m => list[ci].Method)</td>
                                <td scope="row">@Html.DisplayFor(m => list[ci].Date)</td>
                            </tr>
                        }
                    }
                }
            </tbody>
        </table>
    }
}
else
{
    <h3 style="font-family:'Palatino Linotype';color:red">Locations or another records for summary does NOT exist in the table...</h3>
}







